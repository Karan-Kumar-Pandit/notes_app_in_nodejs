const config = {
  databaseURL: "mongodb://0.0.0.0:27017/my_new_notes",
  port: 5000,
};

module.exports = config;

// -----------------------------------------
// File: confige.js
// 1. `const config = {` - Declares a JavaScript object named "config" to store configuration settings.

// 3. `databaseURL: "mongodb://0.0.0.0:27017/my_new_notes",` - Specifies the URL of the MongoDB database.

// 4. `port: 5000,` - Specifies the port number for the server.

// 6. `};` - Closes the "config" object declaration.

// 7. `module.exports = config;` - Exports the "config" object to be used in other files.
